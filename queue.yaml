queue:

- name: docket
  mode: pull

# us_ny - New York (state)
- name: us-ny-scraper
  mode: push
  rate: 18/m
  bucket_size: 1
  max_concurrent_requests: 3
  retry_parameters:
    min_backoff_seconds: 5
    max_backoff_seconds: 300
    task_age_limit: 3d

