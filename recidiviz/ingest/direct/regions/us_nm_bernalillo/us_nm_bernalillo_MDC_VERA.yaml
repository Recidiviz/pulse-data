key_mappings:
  RACE: person.race
  AGE: person.age
  SEX: person.gender

child_key_mappings:
  BOOKING NUMBER: booking.booking_id
  ARRIVAL: booking.admission_date
  STATUS: booking.custody_status
  FACILITY: booking.facility
  ARRESTING AGENCY: arrest.agency
  CHARGE: charge.name
  BOND: bond.amount
  BOND TYPE: bond.bond_type
  BOND STATUS: bond.status

keys_to_ignore:
  - HOLD
  # Charge ID: charge.charge_id
  # Offense Date: charge.offense_date
  # Charge Status: charge.status
  # Charge Description: charge.name
  # Charge Statute: charge.statute
  # Charge Class: charge.charge_class
  # Charge Degree: charge.degree
  # BondID: bond.bond_id
  # Bond Amount: bond.amount
  # Bond Type: bond.bond_type
  # Bond Status: bond.status

primary_key:
 PERSON ID: person.person_id
