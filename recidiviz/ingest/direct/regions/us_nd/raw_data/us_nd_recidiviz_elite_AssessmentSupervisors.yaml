# yaml-language-server: $schema=./../../../raw_data/yaml_schema/schema.json
file_tag: recidiviz_elite_AssessmentSupervisors
file_description: |-
  Unclear what the purpose of this table is. It is a secondary reference table to help 
  decode data included in recidiviz_elite_Assessments - what it serves to decode will need to be
  clarified with DOCR staff.
data_classification: source
primary_key_cols:
  - ASSESSMENT_ID
  - SUPERVISION_LEVEL_TYPE
columns:
  - name: ACTIVE_FLAG
    description: |-
      whether this assessment is still used.
  - name: ASSESSMENT_ID
    description: |-
      The ID of the assessment.
  - name: CREATE_DATETIME
    description: |-
      The date and time this record was created.
    field_type: datetime
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
  - name: CREATE_USER_ID
    description: |-
      The user ID of the user who created this record.
    is_pii: True
  - name: EFFECTIVE_DATE
    description: |-
      TODO(#18645): Fill in with answer from ND
    field_type: datetime
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
  - name: EXPIRY_DATE
    description: |-
      TODO(#18645): Fill in with answer from ND
    field_type: datetime
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
  - name: LIST_SEQ
    description: |-
      TODO(#18645): Fill in with answer from ND
  - name: MAX_SCORE
    description: |-
      TODO(#18645): Fill in with answer from ND
  - name: MINI_SCORE
    description: |-
      TODO(#18645): Fill in with answer from ND
  - name: MODIFY_DATETIME
    description: |-
      The last time this record was modified.
    field_type: datetime
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
  - name: MODIFY_USER_ID
    description: |-
      The last user to modify this record.
    is_pii: True
  - name: SEAL_FLAG
    description: |-
      TODO(#18645): Fill in with answer from ND
  - name: SUPERVISION_LEVEL_TYPE
    description: |-
      TODO(#18645): Fill in with answer from ND
  - name: UPDATE_ALLOWED_FLAG
    description: |-
      Whether or not this record can be updated over time.
update_cadence: IRREGULAR
