key_mappings:
  ParoleNumber: state_person.state_person_id

child_key_mappings:
  ParoleNumber: state_person_external_id.state_person_external_id_id
  InterviewDate: state_assessment.assessment_date
  LSIRScore: state_assessment.assessment_score

primary_key:
  ParoleNumber: state_person.state_person_id

keys_to_ignore:
  - ParoleCountID # Parole count id (meaning unclear) - handled manually in the controller
  - ReleaseStatus # Release status (meaning unclear) - handled manually in the controller
  - LsirID # Index of a particular instance of this assessment taken by a particular person - handled manually in the controller
