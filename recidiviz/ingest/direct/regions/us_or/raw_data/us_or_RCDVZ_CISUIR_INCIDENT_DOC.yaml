# yaml-language-server: $schema=./../../../raw_data/yaml_schema/schema.json
file_tag: RCDVZ_CISUIR_INCIDENT_DOC
file_description: |-
  TODO(#17148): Fill in the file description
data_classification: source
primary_key_cols:
  - DOCUMENT_ID
columns:
  - name: DOCUMENT_ID
    description: |-
      TODO(#17148): Fill in with answer from OR
  - name: INCIDENT_ID
    description: |-
      TODO(#17148): Fill in with answer from OR
  - name: NOTE
    description: |-
      TODO(#17148): Fill in with answer from OR
    is_pii: True
  - name: FILENAME
    description: |-
      TODO(#17148): Fill in with answer from OR
  - name: MIME_TYPE
    description: |-
      TODO(#17148): Fill in with answer from OR
  - name: CREATE_DATE
    field_type: datetime
    description: |-
      TODO(#17148): Fill in with answer from OR
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
      - "SAFE.PARSE_DATETIME('%m/%d/%Y', {col_name})"
  - name: CREATEDBY
    description: |-
      TODO(#17148): Fill in with answer from OR
    is_pii: true
  - name: MODIFY_DATE
    field_type: datetime
    description: |-
      TODO(#17148): Fill in with answer from OR
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
      - "SAFE.PARSE_DATETIME('%m/%d/%Y', {col_name})"
  - name: VERSION_NUM
    description: |-
      TODO(#17148): Fill in with answer from OR
  - name: DELETE_DATE
    field_type: datetime
    description: |-
      TODO(#17148): Fill in with answer from OR
    datetime_sql_parsers:
      - "SAFE.PARSE_DATETIME('%m/%d/%Y %I:%M:%S %p', {col_name})"
      - "SAFE.PARSE_DATETIME('%m/%d/%Y', {col_name})"
