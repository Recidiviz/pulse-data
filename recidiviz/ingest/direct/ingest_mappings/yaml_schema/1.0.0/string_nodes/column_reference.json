{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Evaluates to the value in a single column in the input CSV.",
  "definitions": {
    "manifest": {
      "description": "The value must be a reference to a column in the input CSV.",
      "type": "string",
      "pattern": "^[^$]"
    }
  }
}
