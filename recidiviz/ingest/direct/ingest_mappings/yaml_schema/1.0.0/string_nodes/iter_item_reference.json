{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Evaluates to the value that we are currently iterating over in a $foreach loop. Can only be used within the context of a $foreach node.",
  "definitions": {
    "manifest": {
      "description": "The value must be a reference to a column in the input CSV.",
      "type": "string",
      "const": "$iter_item"
    }
  }
}
