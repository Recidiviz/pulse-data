{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "YAML expressions that can be used to generate an enum value and (optionally) that enum's associated raw text value.",
  "oneOf": [
    {
      "$ref": "../generic_expressions/conditional.json"
    },
    {
      "$ref": "enum_literal.json"
    },
    {
      "$ref": "enum_mapping.json"
    }
  ]
}
