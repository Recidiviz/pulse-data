{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "Evaluates to the same enum value for every input CSV row, regardless of the CSV's contents. Can only be used for enum fields. When this expression is used, the *_raw_text field for the enum will not be hydrated.",
  "definitions": {
    "manifest": {
      "type": "string",
      "pattern": "^\\$literal_enum\\(([^.]+)\\.([^.]+)\\)$",
      "examples": [
        "$literal_enum(StateAgentType.SUPERVISION_OFFICER)"
      ]
    }
  }
}
