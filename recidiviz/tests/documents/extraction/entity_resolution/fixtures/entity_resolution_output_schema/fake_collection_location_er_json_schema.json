{
  "type": "object",
  "description": "The set of distinct locations resolved from a single composite document's numbered entries.",
  "properties": {
    "entities": {
      "type": "array",
      "description": "Each element is a distinct real-world location (an \"entity\"). If two would have identical entity-field values, they are the same location \u2014 emit one element, not two.",
      "minItems": 1,
      "items": {
        "type": "object",
        "description": "A single element of the array.",
        "properties": {
          "entity_id": {
            "type": "integer",
            "description": "Index of this entity in the entities array (using 1-indexing)."
          },
          "location": {
            "type": [
              "string",
              "null"
            ],
            "description": "The location associated with the record."
          },
          "entry_nums": {
            "type": "array",
            "description": "The entry numbers ([Entry N]) that refer to this entity. Across all entities, assign every entry in the document to exactly one entity, and never use a number with no matching entry in the document.",
            "minItems": 1,
            "items": {
              "type": "integer",
              "description": "A single integer element of the array."
            }
          }
        },
        "required": [
          "entity_id",
          "location",
          "entry_nums"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [
    "entities"
  ],
  "additionalProperties": false
}
