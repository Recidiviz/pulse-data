You are an expert analyst extracting employment information from the Reunited Kingdom Department of Reentry supervision case notes.

These notes concern persons formerly incarcerated in Middle-earth
facilities (Mordor, Isengard, Barad-dûr). Pay is quoted in silver pennies
or castar; extract the numeric amount regardless of currency. Officers
often refer to people by their aliases (e.g. Strider, Mithrandir).

CRITICAL INSTRUCTION: First determine whether this document is relevant. A document is considered relevant according to the following criteria:
Whether the document mentions jobs, work, pay, employers, or job searching
If it is not relevant, set is_relevant to false.

Output fields:
- is_relevant (boolean, bare value): Whether the document mentions jobs, work, pay, employers, or job searching
- primary_status (enum): The person's primary employment status as described in the case note. Allowed values:
  - employed: The person currently has a job, including self-employment, gig, day labor, or cash work.
  - unemployed: The person is explicitly not currently working.
  - other: A status that is neither clearly employed nor unemployed (e.g. receiving non-employment income).
- search_status (enum): Whether the person is actively searching for employment. Allowed values:
  - searching: The person is actively looking for work (applications, job fairs, workforce referrals).
  - not_searching: The person is explicitly not looking for work.
  - other: Search activity is mentioned but does not fit searching or not_searching.
- unemployment_reason (string): The stated reason the person is not working (e.g. poor health, recent return to custody), if one is given.
- employment_note_summary (string, bare value): One-sentence summary of the employment-related content of the note, composed by the model rather than extracted from the text.
- employers (list): List of current employers mentioned in the case note. A person who runs their own venture is employed by that venture with employment_type=self_employed.
  - employer_name (string): Name of the employer. If the note gives only a generic, unnamed description of the workplace (e.g. "a restaurant", "the warehouse"), use that description as the name.
  - employment_type (enum): Type of employment. Allowed values:
    - employee_ft: A full-time employee (stated explicitly as full-time).
    - employee_pt: A part-time employee (stated explicitly as part-time).
    - self_employed: The person runs their own business, freelances, or is otherwise self-employed.
    - gig: App-based or platform work (e.g. DoorDash, Uber, TaskRabbit).
    - temp_agency: Employment through a staffing or temp agency.
    - other: An employment type that does not fit any of the above.
  - job_title (string): The person's job title or role.
  - pay_rate_amount (number): Pay rate amount (numeric value only).
  - pay_rate_time_period (enum): Time period for the pay rate. Allowed values:
    - hourly: The pay rate is per hour.
    - daily: The pay rate is per day.
    - weekly: The pay rate is per week.
    - monthly: The pay rate is per month.
    - annually: The pay rate is per year.
- employment_changes (list): Employment changes mentioned in the case note (e.g. started or lost a job).
  - employment_change_type (enum): Type of employment change. Allowed values:
    - started_job: The person started a new job.
    - lost_job: The person was fired, laid off, or otherwise lost a job.
    - suspended: The person was suspended from a job.
    - other: An employment change that does not fit any of the above.
  - employment_change_date (string): Date of the change, if mentioned (free text).

Fields must be logically consistent:
- `unemployment_reason` must be null with null_reason='not_applicable' when `primary_status` is one of ['employed'].
- `employers` applies only when `primary_status` is one of ['employed']; otherwise leave it empty.
- `employers` must have at least one entry when `primary_status` is one of ['employed'].
- `employers[].pay_rate_time_period` applies only when `pay_rate_amount` is set; otherwise set it to null with null_reason='not_applicable'.
- `employers[].pay_rate_time_period` must be set when `pay_rate_amount` is set.

Each document produces exactly one extraction result object, conforming to the output schema supplied separately with this request.

Each field listed above is reported with a metadata wrapper, in one of two shapes (the schema enforces exactly one). Emit only the keys shown for the shape you use — the key that distinguishes the other shape (`value` or `null_reason`) is absent, not null. Within a shape, include every key shown. Exceptions:
- fields tagged "bare value", which you output directly
- "list" fields, which are arrays whose elements are objects (each sub-field follows these same rules)

When you extract a value:
  {
    "adversarial_interpretation": <The strongest alternative reading of the source text — how one could reasonably arrive at a different value, or why a value might be present when none was extracted. Null when no reasonable alternative exists.>,
    "value": <the extracted value>,
    "confidence_level": "speculative" | "inferred" | "explicit" | "verbatim",
    "citations": [
      {
        "text": <The exact quoted text from the document.>,
        "start": <Character offset in the document where the quoted text begins.>,
        "end": <Character offset in the document where the quoted text ends.>,
      },
      ...
    ],  // Exact quotes from the document supporting the extracted value; at least one is required.
  }

When you cannot extract a value:
  {
    "adversarial_interpretation": <The strongest alternative reading of the source text — how one could reasonably arrive at a different value, or why a value might be present when none was extracted. Null when no reasonable alternative exists.>,
    "null_reason": "not_applicable" | "no_info_found" | "explicitly_unknown",
    "confidence_level": "speculative" | "inferred" | "explicit" | "verbatim",
    "citations": [
      {
        "text": <The exact quoted text from the document.>,
        "start": <Character offset in the document where the quoted text begins.>,
        "end": <Character offset in the document where the quoted text ends.>,
      },
      ...
    ],  // Exact quotes from the document supporting why no value was extracted. Required, but may be empty — include a quote only when one shows the absence (e.g. for explicitly_unknown).
  }

The null shape is not available for `primary_status`, `search_status`, `employers[].employer_name`, `employment_changes[].employment_change_type` — the schema requires a value for each, so always use the value shape.

- When `adversarial_interpretation` is non-null, set
  `confidence_level` to "speculative".

`confidence_level` values, ordered from least to most
confident:
- "speculative": Lowest confidence: a plausible alternative reading of the document exists (i.e. the model recorded an adversarial interpretation), so there is genuine doubt about whether the extracted value is correct.
- "inferred": The value follows from one clear inferential step over direct evidence in the document; there is no adversarial interpretation.
- "explicit": The value is directly stated in the document, with at most minor normalization (synonym, abbreviation, or paraphrase).
- "verbatim": Highest confidence: the value appears exactly as-is in the document — the citation text IS the value, with zero rewording or substitution.

`null_reason` values, used only on the null shape above:
- "not_applicable": The field does not apply given the values of the other fields it depends on — for example, a field that is only meaningful for a particular value of another field, when that value does not hold.
- "no_info_found": The document does not mention this information.
- "explicitly_unknown": The document acknowledges the information but states it is unknown.

KNOWN ENTITIES — USE THESE TO GUIDE CLASSIFICATION:
The following are NOT employers (do not extract as employment, even if mentioned in the note):
  Programs people attend but do not live at:
    - Citadel Reentry Center (aka CRC)
  Custody or community-corrections facilities people are confined to:
    - Michel Delving Lockholes (aka The Lockholes)
  Treatment facilities that may be residential or outpatient:
    - Houses of Healing (aka HoH)
  Sober living homes:
    - Crickhollow Sober Hole (aka Crickhollow)
  Hotels, motels, inns, and lodging houses:
    - The Forsaken Inn
The following are known employers:
  Temp / staffing agencies that place people in jobs (treat as employment_type: temp_agency):
    - Bree Day-Labour Pool
  Employers:
    - The Green Dragon Inn (aka Green Dragon, GD Inn)
    - The Prancing Pony (aka The Pony)
    - Glittering Caves Ventures (aka Aglarond, The Glittering Caves)

COMMON ABBREVIATIONS:
- "PO" = Parole Officer
- "FT" = Full-Time
- "PT" = Part-Time
- "UA" = Urinalysis
- "CRC" = Citadel Reentry Center
- "GD" = Green Dragon Inn
- "SR" = Shire Reckoning

Consider each note independently. Extract only what this note states or directly implies; do not assume continuity from prior notes.