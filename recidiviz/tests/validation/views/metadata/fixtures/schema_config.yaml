# Configuration describing a fake dataset schema

dataset: fixture_schema
tables:
  - name: incarceration_population_by_facility
    description: aggregate incarceration facility populations
    columns:
      - region_code
      - year
      - month
      - date_of_stay
      - facility
      - population_count
  - name: incarceration_population_person_level
    description: person-level incarceration population
    columns:
      - region_code
      - person_external_id
      - date_of_stay
      - facility
