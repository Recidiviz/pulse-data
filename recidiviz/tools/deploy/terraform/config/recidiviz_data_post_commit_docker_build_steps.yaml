# Auto-generated by generate_recidiviz_data_post_commit_docker_build_steps.py.
# Do not edit manually. To regenerate, run:
#   python -m recidiviz.tools.deploy.cloud_build.generate_recidiviz_data_post_commit_docker_build_steps
source:
  connected_repository:
    repository: projects/$PROJECT_ID/locations/us-west1/connections/Github-Helperbot-West1/repositories/Recidiviz-pulse-data
    revision: $COMMIT_SHA
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - a+w
      - /workspace
    id: Give non-root users access to /workspace/ volume
    entrypoint: chmod
  - name: alpine
    args:
      - -c
      - wget -O docker-credential-gcr.tar.gz "https://github.com/GoogleCloudPlatform/docker-credential-gcr/releases/download/v2.1.22/docker-credential-gcr_linux_amd64-2.1.22.tar.gz"
        && tar xz -f docker-credential-gcr.tar.gz docker-credential-gcr && chmod +x
        docker-credential-gcr && mkdir /workspace/gcloud && mv docker-credential-gcr
        /workspace/gcloud
    id: download-docker-credential
    entrypoint: sh
  - name: gcr.io/cloud-builders/docker
    args:
      - run
      - --privileged
      - --rm
      - tonistiigi/binfmt
      - --install
      - arm64
    id: setup-qemu
  - name: gcr.io/cloud-builders/docker
    args:
      - buildx
      - create
      - --name
      - appengine
    id: create-build-context-appengine
    wait_for:
      - download-docker-credential
      - setup-qemu
  - name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
      - CACHE_SCOPE_KEY=$BRANCH_NAME
    args:
      - -c
      - 'export PATH="/workspace/gcloud:$${PATH}" && /workspace/gcloud/docker-credential-gcr
        configure-docker --registries=us-docker.pkg.dev && SAFE_CACHE_SCOPE_KEY=$$(echo
        "$$CACHE_SCOPE_KEY" | tr "/." "--") && docker buildx build . -f Dockerfile
        --builder appengine --tag=us-docker.pkg.dev/$PROJECT_ID/appengine/build:$COMMIT_SHA
        --cache-to type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/appengine/build:cache-$${SAFE_CACHE_SCOPE_KEY},mode=max
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/appengine/build:cache-$${SAFE_CACHE_SCOPE_KEY}
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/appengine/build:cache-main
        --push --platform=linux/amd64,linux/arm64 --target=recidiviz-app '
    id: build-appengine
    wait_for:
      - create-build-context-appengine
    entrypoint: sh
  - name: gcr.io/cloud-builders/docker
    args:
      - buildx
      - create
      - --name
      - recidiviz-base
    id: create-build-context-recidiviz-base
    wait_for:
      - download-docker-credential
      - setup-qemu
  - name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
      - CACHE_SCOPE_KEY=$BRANCH_NAME
    args:
      - -c
      - 'export PATH="/workspace/gcloud:$${PATH}" && /workspace/gcloud/docker-credential-gcr
        configure-docker --registries=us-docker.pkg.dev && SAFE_CACHE_SCOPE_KEY=$$(echo
        "$$CACHE_SCOPE_KEY" | tr "/." "--") && docker buildx build . -f Dockerfile.recidiviz-base
        --builder recidiviz-base --tag=us-docker.pkg.dev/$PROJECT_ID/recidiviz-base/build:$COMMIT_SHA
        --cache-to type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/recidiviz-base/build:cache-$${SAFE_CACHE_SCOPE_KEY},mode=max
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/recidiviz-base/build:cache-$${SAFE_CACHE_SCOPE_KEY}
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/recidiviz-base/build:cache-main
        --push --platform=linux/amd64,linux/arm64 '
    id: build-recidiviz-base
    wait_for:
      - create-build-context-recidiviz-base
    entrypoint: sh
  - name: gcr.io/cloud-builders/docker
    args:
      - buildx
      - create
      - --name
      - asset-generation
    id: create-build-context-asset-generation
    wait_for:
      - download-docker-credential
      - setup-qemu
  - name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
      - CACHE_SCOPE_KEY=$BRANCH_NAME
    args:
      - -c
      - 'export PATH="/workspace/gcloud:$${PATH}" && /workspace/gcloud/docker-credential-gcr
        configure-docker --registries=us-docker.pkg.dev && SAFE_CACHE_SCOPE_KEY=$$(echo
        "$$CACHE_SCOPE_KEY" | tr "/." "--") && docker buildx build . -f Dockerfile.asset-generation
        --builder asset-generation --tag=us-docker.pkg.dev/$PROJECT_ID/asset-generation/build:$COMMIT_SHA
        --cache-to type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/asset-generation/build:cache-$${SAFE_CACHE_SCOPE_KEY},mode=max
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/asset-generation/build:cache-$${SAFE_CACHE_SCOPE_KEY}
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/asset-generation/build:cache-main
        --push --platform=linux/amd64,linux/arm64 '
    id: build-asset-generation
    wait_for:
      - create-build-context-asset-generation
    entrypoint: sh
  - name: gcr.io/cloud-builders/docker
    args:
      - buildx
      - create
      - --name
      - case-triage-pathways
    id: create-build-context-case-triage-pathways
    wait_for:
      - download-docker-credential
      - setup-qemu
  - name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
      - CACHE_SCOPE_KEY=$BRANCH_NAME
    args:
      - -c
      - 'export PATH="/workspace/gcloud:$${PATH}" && /workspace/gcloud/docker-credential-gcr
        configure-docker --registries=us-docker.pkg.dev && SAFE_CACHE_SCOPE_KEY=$$(echo
        "$$CACHE_SCOPE_KEY" | tr "/." "--") && docker buildx build . -f Dockerfile.case-triage-pathways
        --builder case-triage-pathways --tag=us-docker.pkg.dev/$PROJECT_ID/case-triage-pathways/build:$COMMIT_SHA
        --cache-to type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/case-triage-pathways/build:cache-$${SAFE_CACHE_SCOPE_KEY},mode=max
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/case-triage-pathways/build:cache-$${SAFE_CACHE_SCOPE_KEY}
        --cache-from type=registry,ref=us-docker.pkg.dev/$PROJECT_ID/case-triage-pathways/build:cache-main
        --push --platform=linux/amd64,linux/arm64 '
    id: build-case-triage-pathways
    wait_for:
      - create-build-context-case-triage-pathways
      - build-recidiviz-base
    entrypoint: sh
timeout: 3600s
logs_bucket: gs://${_PROJECT_ID}-ci-cd-logs
options:
  machine_type: E2_HIGHCPU_32
  substitution_option: ALLOW_LOOSE
substitutions:
  _PROJECT_ID: $PROJECT_ID
  _COMMIT_REF: $COMMIT_SHA
  _VERSION_TAG: $COMMIT_SHA
tags:
  - $COMMIT_SHA
  - $COMMIT_SHA
  - BuildImages
